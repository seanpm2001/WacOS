// Start of script
// Written in Portable C
// WacOS Installer demo May 25th 2021 sample build AWOS01 - Tuesday May 25th 2021 at 6:42 pm
#include <stdio.h>
#include <fileSystemOptions.h> // Imports a script that gives file system options
#include <linuxKernelAcesss.h> // Imports a script to access the Linux kernel directly
// #include <usplash.h> // Imports Usplash
#include <plymouthboot.h> // Imports a script to get access to the Plymouth bootsplash
#include <GRUB.h> // Imports a script for emergency access to GNU GRUB
// I am not very experienced with writing in C, so this may come out as bad non-functional code.
return main();
int main(void)
{
  return <linuxKernelAccess.h>
  return <plymouthboot.h>
  return <GRUB.h>
  printf("WacOS Installer\n\nYou are about to install WacOS, the Linux distribution for Apple fans and Linux users going through an Apple phase\nThis program is still experimental, and you may want to install inside a virtual machine.\n[EXIT] [CONTINUE TO INSTALL]");
  bool installConfirm("installConfirm"=false)
  if installConfirm == true; {
    return <fileSystemOptions.h>
    break
    if continue; {
      return <install.h>
      break
      }
   }
}
/* File info
* File type: C Source file (*.c)
* File version: 1 (Tuesday, May 25th 2021 at 6:31 pm)
* Line count (including blank lines and compiler line): 34
*/
// End of script
