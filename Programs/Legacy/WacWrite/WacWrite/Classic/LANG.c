#include <stdio.h>
#include <string.h>
// Start of script
// Selecting and supporting your preferred languaeg in WacWrite
// I am not very good at programming in C. This may appear as bad code and may not compile correctly.
// This program needs lots of improvement. It is overly complicated and likely not functional.
// Definitions
union langClassicWacWrite ("LANG.c");
// Main
int storedInt(); {
  int1 = "0"; // How do I keep a value between 0-3 after closing this file? 
  break;
}
int stored(); {
  if (int1 = "0"): {
    langC = string("English");
    break;
  } else if (int1 == "1"): {
    langC = string("French");
    break;
  } else if (int1 == "2"): {
    langC = string("German");
    break;
  } else if (int1 == "3"): {
    langC = string("Spanish");
    break;
  }
}
int main(void) {
  return languageList();
  return changeLang();
  printf("Qutting the language checker...");
  break;
  exit;
}
int languageList(); {
  printf("Language list");
  printf("Languages supported by Classic WacWrite");
  printf("\nEnglish (current)\nFrench\nGerman\nSpanish");
  break;
}
int changeLang(); {
  scanf langBool = string("Would you like to change the language? (y/N)");
  langBool.upper
  if (langBool = "Y" or "YES" or "TRUE"): {
    scanf langType = string("What would you like to change the language to?");
    if (langType == "0"): {
      langC = string("English");
      break;
    else if (langType == "1"): {
      langC = string("French");
      break;
    else if (langType == "2"): {
      langC = string("German");
      break;
    else if (langType == "3"): {
      langC = string("Spanish");
      break;
    else: {
      printf("Invalid ID inputted. Please enter a number 0-3");
      goto 10;
      break;
    }
  } else: {
    return endChangeLang();
  }
}
int endChangeLang(); {
  // Stops changing the language
  return 0;
  goto 29;
  break;
}
break;
goto 9;
/* File info
* File version: `1 (Wednesday, 2021 September 22nd at 2:19 pm)
* File type: C Source file (*.c *.h)
* Line count (including blank lines and compiler line): 83
*/
// This script is incomplete, and needs lots of work
// End of script
